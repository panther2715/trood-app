import 'package:flutter/material.dart';

class TroodTab extends StatelessWidget {
  const TroodTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
 backgroundColor: const Color(0xFF121212),
      
    );
  }
}